{
  "name": "calendar-alarm",
  "version": "2.2.0",
  "private": true,
  "description": "A privacy-first Google Calendar reminder amplifier.",
  "author": "Dustin Davis",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/djedi/gcal-meeting-alerter.git"
  },
  "homepage": "https://github.com/djedi/gcal-meeting-alerter#readme",
  "bugs": {
    "url": "https://github.com/djedi/gcal-meeting-alerter/issues"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "check": "node --check alert-core.js && node --check calendar-tabs.js && node --check snooze.js && node --check background.js && node --check content.js && node --check page-bridge.js && node --check popup.js && node --check options.js && node --check interrupt.js && node --check scripts/build-release.mjs && npm test",
    "package": "node scripts/build-release.mjs",
    "release": "npm run check && npm run package"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
